# ═══════════════════════════════════════════════════════════════
# MedAI Environment Variables (root)
# Copy this file to .env and fill in values:
#   cp .env.example .env
# ═══════════════════════════════════════════════════════════════

# Backend API URL — used by React frontend (VITE_ prefix required)
# For local dev:  http://localhost:8000
# For Docker:     leave empty — Nginx proxies /api to backend
VITE_API_URL=http://localhost:8000

# ── Google Sign-In (optional) ────────────────────────────────
# Create a project at https://console.cloud.google.com
# → APIs & Services → Credentials → OAuth 2.0 Client ID (Web application)
# Add your domain to "Authorized JavaScript origins"
VITE_GOOGLE_CLIENT_ID=

# ── UPI / Wallet Payments ────────────────────────────────────
# ⚠️  Set these in Render → Environment (NEVER commit real values)
# Your UPI ID — e.g. yourphone@mbk (Mobiquick), @ybl (PhonePe), @oksbi (GPay)
VITE_UPI_ID=yourphone@mbk
# Name shown on the QR / confirmation screen
VITE_MERCHANT_NAME=Your Name
# Optional: email shown on payment profile card
VITE_MERCHANT_EMAIL=your@email.com
# Optional: phone shown on payment profile card
VITE_MERCHANT_PHONE=XXXXXXXXXX
# WhatsApp for payment alert notifications — country code + number, no + or spaces
# India example: 91 followed by 10-digit mobile
VITE_WHATSAPP_NO=91XXXXXXXXXX

# ngrok Auth Token — enables public internet access via Docker
# Get yours free at: https://dashboard.ngrok.com/get-started/your-authtoken
# Leave blank to skip ngrok (app works locally at http://localhost)
NGROK_AUTHTOKEN=
